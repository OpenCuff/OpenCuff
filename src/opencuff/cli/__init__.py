"""OpenCuff CLI module.

This module provides the command-line interface for OpenCuff, enabling users to:
    - Initialize projects with `cuff init`
    - Check plugin status with `cuff status`
    - Diagnose issues with `cuff doctor`
    - Interact with plugins directly
"""
